

pipeline {
    agent {
        label 'Built-In-Node'
          }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
